#pragma once

#include "Drawable.h"

#include <string>
/*
namespace sgl
{
	namespace internals
	{
		enum class EditCommand
		{
			MoveCursorLeft
		};
		class EditableText : public Drawable
		{
		public:
			explicit EditableText(const std::string& text);

			void processKeyPress(SDL_Keysym key);
			std::string getText() const;
			int getLength() const;
			bool isEmpty() const;
			int getCursorPosition() const;
			void setCursorPosition();
			virtual void draw(SDL_Renderer* renderer) override;


		private:
			std::string firstPart_;
			std::string selection_;
			std::string lastPart_;
		};
	}
}
*/